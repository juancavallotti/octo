{
  "blocks": [
    {
      "type": "set-payload",
      "label": "Set Payload",
      "category": "processor",
      "group": "Data",
      "icon": "Wand2",
      "description": "Replace the message body with the result of a CEL expression.",
      "fields": [
        {
          "name": "value",
          "label": "Value",
          "type": "cel",
          "required": true,
          "description": "CEL expression. Sees body, vars, eventID, correlationID."
        }
      ]
    },
    {
      "type": "set-variable",
      "label": "Set Variable",
      "category": "processor",
      "group": "Data",
      "icon": "Variable",
      "description": "Store the result of a CEL expression in a named variable.",
      "fields": [
        {
          "name": "name",
          "label": "Name",
          "type": "string",
          "required": true,
          "description": "Variable name to set."
        },
        {
          "name": "value",
          "label": "Value",
          "type": "cel",
          "required": true,
          "description": "CEL expression evaluated and stored under the variable name."
        }
      ]
    },
    {
      "type": "delete-variable",
      "label": "Delete Variable",
      "category": "processor",
      "group": "Data",
      "icon": "Trash2",
      "description": "Remove a named variable from the message.",
      "fields": [
        {
          "name": "name",
          "label": "Name",
          "type": "string",
          "required": true,
          "description": "Variable name to remove."
        }
      ]
    },
    {
      "type": "multi-transform",
      "label": "Multi Transform",
      "category": "processor",
      "group": "Data",
      "icon": "Layers",
      "description": "Apply an ordered list of additive CEL edits in one block. Each step sets the body (setBody) or a variable (setVar/value); the edits accumulate, so a later step sees the results of the earlier ones. Compresses a chain of set-payload / set-variable blocks into one.",
      "fields": [
        {
          "name": "transforms",
          "label": "Transforms",
          "type": "transform-list",
          "required": true,
          "description": "Ordered list of edits. Each entry is either {setBody: <CEL>} to replace the body, or {setVar: <name>, value: <CEL>} to set a variable. Applied top to bottom; later expressions see body/vars produced by earlier steps. Expressions see body, vars, eventID, correlationID, env, now."
        }
      ]
    },
    {
      "type": "template-resource",
      "label": "Template Resource",
      "category": "processor",
      "group": "Data",
      "icon": "FileText",
      "description": "Render a declared template resource against the current message. Writes the rendered text to the message body by default, or to a variable when a target is set. The template is declared under resources.templates and embeds {{ CEL }} over body, vars, env, now.",
      "fields": [
        {
          "name": "id",
          "label": "Template",
          "type": "string",
          "required": true,
          "ref": { "kind": "template" },
          "description": "The template to render: its resources.templates alias (the `as`), or its resource path when unaliased."
        },
        {
          "name": "target",
          "label": "Target variable",
          "type": "string",
          "required": false,
          "description": "Variable to store the rendered text in. Leave empty to replace the message body."
        }
      ]
    },
    {
      "type": "log",
      "label": "Log",
      "category": "processor",
      "group": "Data",
      "icon": "ScrollText",
      "description": "Wire-tap that logs the message and passes it through unchanged.",
      "fields": [
        {
          "name": "message",
          "label": "Message",
          "type": "cel",
          "required": false,
          "description": "CEL expression to log. Defaults to the JSON body."
        },
        {
          "name": "level",
          "label": "Level",
          "type": "enum",
          "required": false,
          "default": "info",
          "enum": ["debug", "info", "warn", "error"],
          "description": "Log level."
        },
        {
          "name": "logger",
          "label": "Logger",
          "type": "string",
          "required": false,
          "description": "Optional named logger connector to write to.",
          "ref": { "kind": "connector", "connectorType": "logger" }
        },
        {
          "name": "full",
          "label": "Full message",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Attach the whole message (ids, vars, body) as structured log attributes."
        }
      ]
    },
    {
      "type": "rest",
      "label": "REST Call",
      "category": "processor",
      "group": "Integration",
      "icon": "Globe",
      "description": "Make an HTTP request through an http-client connector.",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the http-client connector to use.",
          "ref": { "kind": "connector", "connectorType": "http-client" }
        },
        {
          "name": "method",
          "label": "Method",
          "type": "enum",
          "required": false,
          "default": "GET",
          "enum": ["GET", "POST", "PUT", "PATCH", "DELETE", "HEAD"],
          "description": "HTTP method."
        },
        {
          "name": "path",
          "label": "Path",
          "type": "string",
          "required": false,
          "description": "Path appended to the connector base URL."
        },
        {
          "name": "query",
          "label": "Query",
          "type": "string-map",
          "required": false,
          "description": "Query params; each value is a CEL expression."
        },
        {
          "name": "headers",
          "label": "Headers",
          "type": "string-map",
          "required": false,
          "description": "Request headers; each value is a CEL expression."
        },
        {
          "name": "body",
          "label": "Body",
          "type": "cel",
          "required": false,
          "description": "CEL expression for the request body."
        },
        {
          "name": "failOnError",
          "label": "Fail on error",
          "type": "boolean",
          "required": false,
          "default": true,
          "description": "Turn a 400+ status into a flow error."
        },
        {
          "name": "statusVar",
          "label": "Status variable",
          "type": "string",
          "required": false,
          "default": "statusCode",
          "description": "Variable to store the response status code in."
        }
      ]
    },
    {
      "type": "sql",
      "label": "SQL",
      "category": "processor",
      "group": "Integration",
      "icon": "Database",
      "description": "Execute a SQL statement against a database connector.",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the database connector to use.",
          "ref": { "kind": "connector", "connectorType": "database" }
        },
        {
          "name": "query",
          "label": "Query",
          "type": "string",
          "required": true,
          "description": "SQL statement with placeholders."
        },
        {
          "name": "args",
          "label": "Arguments",
          "type": "string-list",
          "required": false,
          "description": "CEL expressions bound to the statement placeholders."
        },
        {
          "name": "exec",
          "label": "Exec mode",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Use ExecContext (no result set); result becomes {rowsAffected: N}."
        },
        {
          "name": "single",
          "label": "Single row",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "In query mode, unwrap the result to the first row object."
        }
      ]
    },
    {
      "type": "flow-ref",
      "label": "Flow Reference",
      "category": "processor",
      "group": "Integration",
      "icon": "Webhook",
      "description": "Invoke another flow by name.",
      "fields": [
        {
          "name": "flow",
          "label": "Flow",
          "type": "string",
          "required": true,
          "description": "Name of the flow to invoke.",
          "ref": { "kind": "flow" }
        },
        {
          "name": "oneWay",
          "label": "One-way",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Fire-and-forget if true; otherwise wait and fold the result back in."
        }
      ]
    },
    {
      "type": "handle-errors",
      "label": "Handle Errors",
      "category": "control-flow",
      "group": "Flow Control",
      "icon": "ShieldAlert",
      "description": "Run the process chain; on error, expose vars.error and run the error chain (recovery).",
      "fields": [
        {
          "name": "process",
          "label": "Process",
          "type": "block-list",
          "required": true,
          "description": "The happy-path chain."
        },
        {
          "name": "error",
          "label": "Error",
          "type": "block-list",
          "required": true,
          "description": "Runs when the process chain errors; reads vars.error."
        }
      ]
    },
    {
      "type": "fork",
      "label": "Fork",
      "category": "control-flow",
      "group": "Flow Control",
      "icon": "GitFork",
      "description": "Scatter the message across parallel branches, then join and pass through.",
      "fields": [
        {
          "name": "branches",
          "label": "Branches",
          "type": "flow-list",
          "required": true,
          "description": "Parallel flows; each receives a clone of the message."
        }
      ]
    },
    {
      "type": "if",
      "label": "If",
      "category": "control-flow",
      "group": "Flow Control",
      "icon": "Split",
      "description": "Conditional branching on a CEL boolean expression.",
      "fields": [
        {
          "name": "condition",
          "label": "Condition",
          "type": "cel",
          "required": true,
          "description": "CEL boolean expression."
        },
        {
          "name": "then",
          "label": "Then",
          "type": "flow",
          "required": true,
          "description": "Flow run when the condition is true."
        },
        {
          "name": "else",
          "label": "Else",
          "type": "flow",
          "required": false,
          "description": "Flow run when the condition is false."
        }
      ]
    },
    {
      "type": "switch",
      "label": "Switch",
      "category": "control-flow",
      "group": "Flow Control",
      "icon": "Split",
      "description": "Multi-case routing; runs the first matching case or the default.",
      "fields": [
        {
          "name": "cases",
          "label": "Cases",
          "type": "case-list",
          "required": true,
          "description": "Ordered, condition-guarded flows (each has a CEL `when`)."
        },
        {
          "name": "default",
          "label": "Default",
          "type": "flow",
          "required": false,
          "description": "Flow run when no case matches."
        }
      ]
    },
    {
      "type": "foreach",
      "label": "For Each",
      "category": "control-flow",
      "group": "Flow Control",
      "icon": "Repeat",
      "description": "Sequentially iterate over an array, running the body per element.",
      "fields": [
        {
          "name": "items",
          "label": "Items",
          "type": "cel",
          "required": true,
          "description": "CEL expression evaluating to the array to iterate."
        },
        {
          "name": "as",
          "label": "As",
          "type": "string",
          "required": false,
          "default": "item",
          "description": "Variable name bound to each element."
        },
        {
          "name": "body",
          "label": "Body",
          "type": "flow",
          "required": true,
          "description": "Flow run once per element."
        }
      ]
    },
    {
      "type": "enrich",
      "label": "Enrich",
      "category": "control-flow",
      "group": "Flow Control",
      "icon": "Sparkles",
      "description": "Run a body flow on an isolated copy of the message, then enrich the message from the result: a CEL expression for the body and a CEL expression per variable.",
      "fields": [
        {
          "name": "setBody",
          "label": "Set body",
          "type": "cel",
          "required": false,
          "description": "CEL expression for the new body, evaluated against the scope's result (the enriched clone). Empty leaves the body unchanged."
        },
        {
          "name": "setVars",
          "label": "Set variables",
          "type": "string-map",
          "required": false,
          "description": "Map of variable name to CEL expression, each evaluated against the scope's result and set on the message."
        },
        {
          "name": "body",
          "label": "Body",
          "type": "flow",
          "required": true,
          "description": "Flow run once on an isolated clone of the message."
        }
      ]
    },
    {
      "type": "ai-mapping",
      "label": "AI Mapping",
      "category": "processor",
      "group": "AI & LLM",
      "icon": "Sparkles",
      "description": "Reshape the message body to a target shape described by a prompt; validates against an optional output schema and errors on failure.",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the LLM connector to use.",
          "ref": { "kind": "connector", "connectorCategory": "llm" }
        },
        {
          "name": "prompt",
          "label": "Prompt",
          "type": "string",
          "required": true,
          "description": "Instruction describing how to reshape the body."
        },
        {
          "name": "inputExample",
          "label": "Input example",
          "type": "string",
          "required": false,
          "description": "Example input payload that guides recognition (JSON)."
        },
        {
          "name": "outputExample",
          "label": "Output example",
          "type": "string",
          "required": false,
          "description": "Example output payload that shapes the result (JSON)."
        },
        {
          "name": "outputSchema",
          "label": "Output schema",
          "type": "string",
          "required": false,
          "description": "JSON Schema the result is validated against; a failure errors the block."
        },
        {
          "name": "maxTokens",
          "label": "Max tokens",
          "type": "number",
          "required": false,
          "description": "Response token cap for this call (0 = connector default)."
        }
      ]
    },
    {
      "type": "ai-retry",
      "label": "AI Retry",
      "category": "control-flow",
      "group": "AI & LLM",
      "icon": "RefreshCw",
      "description": "Run the process chain; on error, let the LLM inspect vars.error, revise the message, and retry up to maxAttempts, then run the error chain.",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the LLM connector that revises the message between attempts.",
          "ref": { "kind": "connector", "connectorCategory": "llm" }
        },
        {
          "name": "prompt",
          "label": "Prompt",
          "type": "string",
          "required": true,
          "description": "Instruction for repairing the message from vars.error before a retry."
        },
        {
          "name": "maxAttempts",
          "label": "Max attempts",
          "type": "number",
          "required": false,
          "default": 3,
          "description": "How many times to revise and re-run the process chain."
        },
        {
          "name": "process",
          "label": "Process",
          "type": "block-list",
          "required": true,
          "description": "The protected chain, re-run after each revision."
        },
        {
          "name": "error",
          "label": "Error",
          "type": "block-list",
          "required": false,
          "description": "Runs when attempts are exhausted; reads vars.error."
        }
      ]
    },
    {
      "type": "ai-router",
      "label": "AI Router",
      "category": "control-flow",
      "group": "AI & LLM",
      "icon": "Route",
      "description": "Route the message to a named branch the LLM chooses after inspecting it; the default path is the guardrail taken when it is not confident.",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the LLM connector that picks the route.",
          "ref": { "kind": "connector", "connectorCategory": "llm" }
        },
        {
          "name": "prompt",
          "label": "Prompt",
          "type": "string",
          "required": true,
          "description": "Routing instruction; the model reads the body/variables before choosing."
        },
        {
          "name": "guardrail",
          "label": "Guardrail",
          "type": "string",
          "required": false,
          "description": "Describes when to fall back to the default path (low confidence / ambiguity)."
        },
        {
          "name": "routes",
          "label": "Routes",
          "type": "route-list",
          "required": true,
          "description": "Named, described branches; the model picks one by name."
        },
        {
          "name": "default",
          "label": "Default",
          "type": "flow",
          "required": false,
          "description": "The guardrail path, run when the model is not confident."
        }
      ]
    },
    {
      "type": "ai-agent",
      "label": "AI Agent",
      "category": "control-flow",
      "group": "AI & LLM",
      "icon": "Bot",
      "description": "Let the LLM call branches as tools in a loop to accomplish a task; tool runs share accumulating variables. The default path is the guardrail.",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the LLM connector that drives the agent loop.",
          "ref": { "kind": "connector", "connectorCategory": "llm" }
        },
        {
          "name": "prompt",
          "label": "Prompt",
          "type": "string",
          "required": true,
          "description": "Task instruction; the model calls tools as needed, then stops."
        },
        {
          "name": "guardrail",
          "label": "Guardrail",
          "type": "string",
          "required": false,
          "description": "Describes when to fall back to the default path (cannot complete with confidence)."
        },
        {
          "name": "maxIterations",
          "label": "Max iterations",
          "type": "number",
          "required": false,
          "description": "Cap on tool-calling turns before falling back to the guardrail."
        },
        {
          "name": "memoryThreadId",
          "label": "Memory thread ID",
          "type": "cel",
          "required": false,
          "description": "CEL expression for the conversation thread id. When set, the agent loads the thread's prior transcript before its run and saves it after; empty disables memory."
        },
        {
          "name": "memoryMaxTokens",
          "label": "Memory max tokens",
          "type": "number",
          "required": false,
          "default": 8000,
          "description": "Estimated-token budget for the stored transcript; it is compacted when it exceeds this."
        },
        {
          "name": "memoryCompaction",
          "label": "Memory compaction",
          "type": "enum",
          "required": false,
          "default": "prune",
          "enum": ["prune", "summarize"],
          "description": "How to shrink memory over budget: prune drops the oldest turns; summarize folds them into a running summary."
        },
        {
          "name": "tools",
          "label": "Tools",
          "type": "tool-list",
          "required": true,
          "description": "Named, described branches wired to the model as callable functions."
        },
        {
          "name": "default",
          "label": "Default",
          "type": "flow",
          "required": false,
          "description": "The guardrail path, run when the agent cannot complete the task."
        }
      ]
    },
    {
      "type": "clear-agent-memory",
      "label": "Clear Agent Memory",
      "category": "processor",
      "group": "AI & LLM",
      "icon": "BrainCog",
      "description": "Erase an ai-agent conversation thread's stored memory by its thread id.",
      "fields": [
        {
          "name": "threadId",
          "label": "Thread ID",
          "type": "cel",
          "required": true,
          "description": "CEL expression for the thread id whose memory is cleared."
        }
      ]
    },
    {
      "type": "cache-scope",
      "label": "Cache",
      "category": "control-flow",
      "group": "Storage & Cache",
      "icon": "Clock",
      "description": "Memoize the body flow's result in the runtime store, keyed by a CEL expression.",
      "fields": [
        {
          "name": "key",
          "label": "Cache Key",
          "type": "cel",
          "required": true,
          "description": "CEL expression evaluated to the cache key."
        },
        {
          "name": "ttl",
          "label": "TTL",
          "type": "string",
          "required": false,
          "default": "60s",
          "description": "How long an entry stays fresh (e.g. '5m'); '0' never expires."
        },
        {
          "name": "body",
          "label": "Body",
          "type": "flow",
          "required": true,
          "description": "Flow whose result is cached and replayed on a hit."
        }
      ]
    },
    {
      "type": "invalidate-cache",
      "label": "Invalidate Cache",
      "category": "processor",
      "group": "Storage & Cache",
      "icon": "Eraser",
      "description": "Evict a cache-scope entry by its key so the next run recomputes.",
      "fields": [
        {
          "name": "key",
          "label": "Cache Key",
          "type": "cel",
          "required": true,
          "description": "CEL expression evaluated to the cache key to evict."
        }
      ]
    },
    {
      "type": "object-read",
      "label": "Object Read",
      "category": "processor",
      "group": "Storage & Cache",
      "icon": "HardDriveDownload",
      "description": "Read an object from the runtime store into the body or a variable. When the key is absent and `default` is set, the default is folded in exactly like a hit; otherwise the body is nulled (body mode) or the variable is left unset. Set `existsVar` to also record whether the key was found.",
      "fields": [
        {
          "name": "key",
          "label": "Key",
          "type": "cel",
          "required": true,
          "description": "CEL expression evaluated to the object key."
        },
        {
          "name": "as",
          "label": "As variable",
          "type": "string",
          "required": false,
          "description": "Variable to store the object under; empty replaces the body."
        },
        {
          "name": "default",
          "label": "Default",
          "type": "cel",
          "required": false,
          "description": "CEL expression evaluated when the key is absent; its result is folded in like a hit (into the variable, or the body). Empty leaves a miss as a null body / unset variable. Set existsVar to tell a default apart from a hit."
        },
        {
          "name": "existsVar",
          "label": "Exists variable",
          "type": "string",
          "required": false,
          "description": "When set, names a variable the block writes a boolean into: true when the key was found, false when the default/null path was taken. Empty writes no variable (the legacy behavior)."
        }
      ]
    },
    {
      "type": "object-write",
      "label": "Object Write",
      "category": "processor",
      "group": "Storage & Cache",
      "icon": "HardDriveUpload",
      "description": "Write an object to the runtime store under a key.",
      "fields": [
        {
          "name": "key",
          "label": "Key",
          "type": "cel",
          "required": true,
          "description": "CEL expression evaluated to the object key."
        },
        {
          "name": "value",
          "label": "Value",
          "type": "cel",
          "required": false,
          "description": "CEL expression for the value to store; empty stores the whole body."
        }
      ]
    },
    {
      "type": "object-delete",
      "label": "Object Delete",
      "category": "processor",
      "group": "Storage & Cache",
      "icon": "Trash2",
      "description": "Delete an object from the runtime store by key.",
      "fields": [
        {
          "name": "key",
          "label": "Key",
          "type": "cel",
          "required": true,
          "description": "CEL expression evaluated to the object key."
        }
      ]
    },
    {
      "type": "queue-dispatch",
      "label": "Queue Dispatch",
      "category": "processor",
      "group": "Integration",
      "icon": "Send",
      "description": "Send the message to a platform queue subject to load balance work across replicas. By default it publishes fire-and-forget (one-way); enable Await reply to wait for one consumer's reply and fold it back in (request).",
      "fields": [
        {
          "name": "subject",
          "label": "Subject",
          "type": "cel",
          "required": true,
          "description": "CEL expression for the queue subject to send to; enables per-message routing/sharding."
        },
        {
          "name": "awaitReply",
          "label": "Await reply",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Wait for one consumer's reply and fold its body and variables back in (request). When false (the default) the message is published fire-and-forget (one-way)."
        },
        {
          "name": "timeout",
          "label": "Timeout",
          "type": "string",
          "required": false,
          "description": "How long to wait for a reply when Await reply is set (e.g. 30s); defaults to the queue service timeout."
        }
      ]
    },
    {
      "type": "publish-event",
      "category": "processor",
      "group": "Integration",
      "label": "Publish Event",
      "icon": "Radio",
      "description": "Broadcast the current message to a platform topic subject, so every subscriber on that subject receives it (fire-and-forget fan-out). The message passes through unchanged.",
      "fields": [
        {
          "name": "subject",
          "label": "Subject",
          "type": "cel",
          "required": true,
          "description": "CEL expression for the topic subject to broadcast to; enables per-message routing."
        },
        {
          "name": "value",
          "label": "Value",
          "type": "cel",
          "required": false,
          "description": "CEL expression whose result becomes the published body. Empty publishes the whole current body."
        }
      ]
    },
    {
      "type": "slack-send-message",
      "label": "Slack Send Message",
      "category": "processor",
      "group": "Slack",
      "icon": "Slack",
      "description": "Post a message to a Slack channel or user through a slack connector.",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the slack connector to use.",
          "ref": {
            "kind": "connector",
            "connectorType": "slack"
          }
        },
        {
          "name": "target",
          "label": "Target",
          "type": "cel",
          "required": true,
          "description": "CEL expression for the channel ID or user ID (Slack DMs a user)."
        },
        {
          "name": "text",
          "label": "Text",
          "type": "cel",
          "required": false,
          "description": "CEL expression for the message text (required unless blocks is set)."
        },
        {
          "name": "threadTs",
          "label": "Thread timestamp",
          "type": "cel",
          "required": false,
          "description": "CEL expression for a parent message ts, to reply in-thread."
        },
        {
          "name": "blocks",
          "label": "Blocks",
          "type": "cel",
          "required": false,
          "description": "CEL expression producing Block Kit blocks (a list)."
        },
        {
          "name": "failOnError",
          "label": "Fail on error",
          "type": "boolean",
          "required": false,
          "default": true,
          "description": "Turn a Slack API error into a flow error."
        }
      ]
    },
    {
      "type": "slack-verify-request",
      "label": "Slack Verify Request",
      "category": "processor",
      "group": "Slack",
      "icon": "Slack",
      "description": "Verify an inbound Slack request signature and answer the URL-verification challenge.",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the slack connector to use.",
          "ref": {
            "kind": "connector",
            "connectorType": "slack"
          }
        },
        {
          "name": "signatureHeader",
          "label": "Signature variable",
          "type": "string",
          "required": false,
          "default": "X-Slack-Signature",
          "description": "Variable holding Slack's request signature."
        },
        {
          "name": "timestampHeader",
          "label": "Timestamp variable",
          "type": "string",
          "required": false,
          "default": "X-Slack-Request-Timestamp",
          "description": "Variable holding Slack's request timestamp."
        },
        {
          "name": "rawBodyVar",
          "label": "Raw body variable",
          "type": "string",
          "required": false,
          "default": "rawBody",
          "description": "Variable holding the exact request body; must match the http source's rawBodyVar."
        }
      ]
    },
    {
      "type": "slack-event",
      "label": "Slack Event",
      "category": "processor",
      "group": "Slack",
      "icon": "Slack",
      "description": "Normalize a verified Slack event and filter it by type and an optional predicate.",
      "fields": [
        {
          "name": "eventTypes",
          "label": "Event types",
          "type": "string-list",
          "required": false,
          "description": "Allowlist of event types (e.g. app_mention, message); empty allows all."
        },
        {
          "name": "filter",
          "label": "Filter",
          "type": "cel",
          "required": false,
          "description": "CEL predicate over the normalized body; drop the event when false."
        }
      ]
    },
    {
      "type": "slack-lookup-user",
      "label": "Slack Lookup User",
      "category": "processor",
      "group": "Slack",
      "icon": "Slack",
      "description": "Resolve a Slack user into a variable — by email (users.lookupByEmail, the default) or by Slack user id (users.info), selected with `by`.",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the slack connector to use.",
          "ref": {
            "kind": "connector",
            "connectorType": "slack"
          }
        },
        {
          "name": "by",
          "label": "Look up by",
          "type": "enum",
          "required": false,
          "default": "email",
          "enum": ["email", "id"],
          "description": "How to resolve the user: 'email' (users.lookupByEmail, the default) or 'id' (users.info)."
        },
        {
          "name": "email",
          "label": "Email",
          "type": "cel",
          "required": false,
          "description": "CEL expression for the email address to resolve. Required when 'by' is 'email'."
        },
        {
          "name": "user",
          "label": "User ID",
          "type": "cel",
          "required": false,
          "description": "CEL expression for the Slack user id to resolve (e.g. U123). Required when 'by' is 'id'."
        },
        {
          "name": "resultVar",
          "label": "Result variable",
          "type": "string",
          "required": false,
          "default": "slackUser",
          "description": "Variable to store the user object in."
        },
        {
          "name": "failOnError",
          "label": "Fail on error",
          "type": "boolean",
          "required": false,
          "default": true,
          "description": "Turn a Slack API error into a flow error."
        }
      ]
    },
    {
      "type": "slack-add-reaction",
      "label": "Slack Add Reaction",
      "category": "processor",
      "group": "Slack",
      "icon": "Slack",
      "description": "Add an emoji reaction to a message (reactions.add).",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the slack connector to use.",
          "ref": {
            "kind": "connector",
            "connectorType": "slack"
          }
        },
        {
          "name": "channel",
          "label": "Channel",
          "type": "cel",
          "required": true,
          "description": "CEL expression for the message channel ID."
        },
        {
          "name": "timestamp",
          "label": "Timestamp",
          "type": "cel",
          "required": true,
          "description": "CEL expression for the target message ts."
        },
        {
          "name": "emoji",
          "label": "Emoji",
          "type": "cel",
          "required": true,
          "description": "CEL expression for the reaction name without colons (e.g. white_check_mark)."
        },
        {
          "name": "failOnError",
          "label": "Fail on error",
          "type": "boolean",
          "required": false,
          "default": true,
          "description": "Turn a Slack API error into a flow error."
        }
      ]
    },
    {
      "type": "slack-update-message",
      "label": "Slack Update Message",
      "category": "processor",
      "group": "Slack",
      "icon": "Slack",
      "description": "Edit a message the bot posted earlier (chat.update).",
      "fields": [
        {
          "name": "connector",
          "label": "Connector",
          "type": "string",
          "required": true,
          "description": "Name of the slack connector to use.",
          "ref": {
            "kind": "connector",
            "connectorType": "slack"
          }
        },
        {
          "name": "channel",
          "label": "Channel",
          "type": "cel",
          "required": true,
          "description": "CEL expression for the message channel ID."
        },
        {
          "name": "timestamp",
          "label": "Timestamp",
          "type": "cel",
          "required": true,
          "description": "CEL expression for the target message ts."
        },
        {
          "name": "text",
          "label": "Text",
          "type": "cel",
          "required": false,
          "description": "CEL expression for the new text (required unless blocks is set)."
        },
        {
          "name": "blocks",
          "label": "Blocks",
          "type": "cel",
          "required": false,
          "description": "CEL expression producing replacement Block Kit blocks (a list)."
        },
        {
          "name": "failOnError",
          "label": "Fail on error",
          "type": "boolean",
          "required": false,
          "default": true,
          "description": "Turn a Slack API error into a flow error."
        }
      ]
    }
  ],
  "connectors": [
    {
      "type": "queue",
      "label": "Platform Queue",
      "icon": "Inbox",
      "settings": [],
      "sources": [
        {
          "type": "queue",
          "label": "Platform Queue",
          "icon": "Inbox",
          "fields": [
            {
              "name": "subject",
              "label": "Subject",
              "type": "string",
              "required": true,
              "description": "Queue subject to subscribe to. Every replica subscribed to the same subject competes for messages, so each message is handled once across the cluster."
            },
            {
              "name": "listeners",
              "label": "Listeners",
              "type": "number",
              "required": false,
              "description": "Number of concurrent handler goroutines; defaults to the queue service default."
            },
            {
              "name": "timeout",
              "label": "Timeout",
              "type": "string",
              "required": false,
              "description": "How long a handler waits for its flow to finish (e.g. 30s); defaults to the queue service timeout."
            }
          ]
        }
      ]
    },
    {
      "type": "events",
      "label": "Platform Events",
      "icon": "Radio",
      "settings": [],
      "sources": [
        {
          "type": "events",
          "label": "Event subscription",
          "icon": "Radio",
          "fields": [
            {
              "name": "subject",
              "label": "Subject",
              "type": "string",
              "required": true,
              "description": "Topic subject to subscribe to. Every subscriber on the subject (including every replica) receives every message — broadcast fan-out, unlike a queue's competing consumers."
            },
            {
              "name": "listeners",
              "label": "Listeners",
              "type": "number",
              "required": false,
              "description": "Number of concurrent handler goroutines; defaults to the topics service default."
            }
          ]
        }
      ]
    },
    {
      "type": "cron",
      "label": "Cron",
      "icon": "Clock",
      "settings": [],
      "sources": [
        {
          "type": "cron",
          "label": "Cron schedule",
          "icon": "Clock",
          "fields": [
            {
              "name": "schedule",
              "label": "Schedule",
              "type": "string",
              "required": true,
              "description": "Cron expression (6 fields with seconds) or descriptor like @every 2s."
            },
            {
              "name": "payload",
              "label": "Payload",
              "type": "cel",
              "required": false,
              "description": "CEL expression for the message body; sees `now` (fire time)."
            },
            {
              "name": "correlationID",
              "label": "Correlation ID",
              "type": "string",
              "required": false,
              "description": "Static correlation ID for emitted messages."
            }
          ]
        }
      ]
    },
    {
      "type": "http",
      "label": "HTTP Server",
      "icon": "Webhook",
      "settings": [
        {
          "name": "host",
          "label": "Host",
          "type": "string",
          "required": false,
          "description": "Bind address; empty binds all interfaces."
        },
        {
          "name": "port",
          "label": "Port",
          "type": "number",
          "required": false,
          "default": 8080,
          "description": "Bind port; 0 = OS-assigned."
        },
        {
          "name": "basePath",
          "label": "Base path",
          "type": "string",
          "required": false,
          "description": "Prefix for all routes."
        },
        {
          "name": "keepAlive",
          "label": "Keep-alive",
          "type": "boolean",
          "required": false,
          "description": "Enable HTTP keep-alives."
        },
        {
          "name": "requestTimeout",
          "label": "Request timeout",
          "type": "string",
          "required": false,
          "default": "30s",
          "description": "How long a handler waits for the flow to finish."
        },
        {
          "name": "readTimeout",
          "label": "Read timeout",
          "type": "string",
          "required": false,
          "description": "Server read timeout."
        },
        {
          "name": "writeTimeout",
          "label": "Write timeout",
          "type": "string",
          "required": false,
          "description": "Server write timeout."
        },
        {
          "name": "idleTimeout",
          "label": "Idle timeout",
          "type": "string",
          "required": false,
          "description": "Server idle timeout."
        }
      ],
      "sources": [
        {
          "type": "http",
          "label": "HTTP route",
          "icon": "Webhook",
          "fields": [
            {
              "name": "path",
              "label": "Path",
              "type": "string",
              "required": true,
              "description": "Route pattern; {name} params become vars.name."
            },
            {
              "name": "headers",
              "label": "Headers",
              "type": "string-list",
              "required": false,
              "description": "Request header names to copy into variables."
            },
            {
              "name": "correlationIdHeader",
              "label": "Correlation ID header",
              "type": "string",
              "required": false,
              "description": "Header to source the CorrelationID from."
            },
            {
              "name": "rawBodyVar",
              "label": "Raw body variable",
              "type": "string",
              "required": false,
              "description": "Capture the exact request bytes into vars.<name> (e.g. to verify an HMAC signature over the raw body)."
            },
            {
              "name": "timeout",
              "label": "Timeout",
              "type": "string",
              "required": false,
              "description": "Per-route wait for the flow; defaults to the connector request timeout."
            },
            {
              "name": "maxBodyBytes",
              "label": "Max body bytes",
              "type": "number",
              "required": false,
              "default": 1048576,
              "description": "Request body size cap."
            }
          ]
        }
      ]
    },
    {
      "type": "http-client",
      "label": "HTTP Client",
      "icon": "Globe",
      "settings": [
        {
          "name": "baseURL",
          "label": "Base URL",
          "type": "string",
          "required": true,
          "description": "Prepended to each request path."
        },
        {
          "name": "timeout",
          "label": "Timeout",
          "type": "string",
          "required": false,
          "default": "30s",
          "description": "Bounds each request."
        },
        {
          "name": "headers",
          "label": "Headers",
          "type": "string-map",
          "required": false,
          "description": "Applied to every request unless already set."
        },
        {
          "name": "maxResponseBytes",
          "label": "Max response bytes",
          "type": "number",
          "required": false,
          "default": 1048576,
          "description": "Response body size cap."
        },
        {
          "name": "auth",
          "label": "Authentication",
          "type": "object",
          "required": false,
          "description": "Authentication applied to every request.",
          "fields": [
            {
              "name": "type",
              "label": "Type",
              "type": "enum",
              "required": false,
              "enum": ["bearer", "basic", "oauth2"],
              "description": "Authentication scheme."
            },
            {
              "name": "token",
              "label": "Bearer token",
              "type": "string",
              "required": false,
              "showIf": { "field": "type", "equals": "bearer" },
              "description": "Token sent as 'Authorization: Bearer'."
            },
            {
              "name": "username",
              "label": "Username",
              "type": "string",
              "required": false,
              "showIf": { "field": "type", "equals": "basic" },
              "description": "Basic auth username."
            },
            {
              "name": "password",
              "label": "Password",
              "type": "string",
              "required": false,
              "showIf": { "field": "type", "equals": "basic" },
              "description": "Basic auth password."
            },
            {
              "name": "tokenURL",
              "label": "Token URL",
              "type": "string",
              "required": false,
              "showIf": { "field": "type", "equals": "oauth2" },
              "description": "OAuth2 token endpoint (client-credentials grant)."
            },
            {
              "name": "clientID",
              "label": "Client ID",
              "type": "string",
              "required": false,
              "showIf": { "field": "type", "equals": "oauth2" },
              "description": "OAuth2 client identifier."
            },
            {
              "name": "clientSecret",
              "label": "Client secret",
              "type": "string",
              "required": false,
              "showIf": { "field": "type", "equals": "oauth2" },
              "description": "OAuth2 client secret."
            },
            {
              "name": "scopes",
              "label": "Scopes",
              "type": "string-list",
              "required": false,
              "showIf": { "field": "type", "equals": "oauth2" },
              "description": "Requested OAuth2 scopes."
            }
          ]
        }
      ],
      "sources": []
    },
    {
      "type": "database",
      "label": "Database",
      "icon": "Database",
      "settings": [
        {
          "name": "driver",
          "label": "Driver",
          "type": "enum",
          "required": true,
          "enum": ["postgres", "sqlite"],
          "description": "Database driver."
        },
        {
          "name": "dsn",
          "label": "DSN",
          "type": "string",
          "required": true,
          "description": "Data source name (Postgres connection string or SQLite file path)."
        },
        {
          "name": "maxOpenConns",
          "label": "Max open connections",
          "type": "number",
          "required": false,
          "description": "Open connection pool cap (0 = unlimited)."
        },
        {
          "name": "maxIdleConns",
          "label": "Max idle connections",
          "type": "number",
          "required": false,
          "description": "Idle connections kept in the pool."
        },
        {
          "name": "connMaxLifetime",
          "label": "Connection max lifetime",
          "type": "string",
          "required": false,
          "description": "Duration bounding connection reuse (e.g. 5m)."
        }
      ],
      "sources": []
    },
    {
      "type": "logger",
      "label": "Logger",
      "icon": "ScrollText",
      "settings": [
        {
          "name": "output",
          "label": "Output",
          "type": "string",
          "required": false,
          "default": "stdout",
          "description": "stdout, stderr, or a file path."
        },
        {
          "name": "format",
          "label": "Format",
          "type": "enum",
          "required": false,
          "default": "text",
          "enum": ["text", "json"],
          "description": "Log output format."
        },
        {
          "name": "level",
          "label": "Level",
          "type": "enum",
          "required": false,
          "default": "info",
          "enum": ["debug", "info", "warn", "error"],
          "description": "Minimum log level."
        },
        {
          "name": "addSource",
          "label": "Add source",
          "type": "boolean",
          "required": false,
          "default": false,
          "description": "Include source file:line in log records."
        }
      ],
      "sources": []
    },
    {
      "type": "llm-anthropic",
      "label": "Anthropic",
      "icon": "Sparkles",
      "category": "llm",
      "settings": [
        {
          "name": "apiKey",
          "label": "API key",
          "type": "string",
          "required": true,
          "description": "Authenticates with the Anthropic API; source from ${ANTHROPIC_API_KEY}. Never logged."
        },
        {
          "name": "model",
          "label": "Model",
          "type": "string",
          "required": false,
          "default": "claude-sonnet-4-6",
          "description": "Model id."
        },
        {
          "name": "maxTokens",
          "label": "Max tokens",
          "type": "number",
          "required": false,
          "default": 4096,
          "description": "Default response token cap; a request may override it."
        },
        {
          "name": "baseURL",
          "label": "Base URL",
          "type": "string",
          "required": false,
          "description": "Overrides the API endpoint (for proxies or testing)."
        }
      ],
      "sources": []
    },
    {
      "type": "llm-openai",
      "label": "OpenAI",
      "icon": "Sparkles",
      "category": "llm",
      "settings": [
        {
          "name": "apiKey",
          "label": "API key",
          "type": "string",
          "required": true,
          "description": "Authenticates with the OpenAI API; source from ${OPENAI_API_KEY}. Never logged."
        },
        {
          "name": "model",
          "label": "Model",
          "type": "string",
          "required": false,
          "default": "gpt-5.4",
          "description": "Model id."
        },
        {
          "name": "maxTokens",
          "label": "Max tokens",
          "type": "number",
          "required": false,
          "description": "Default response token cap (0 = the model default); a request may override it."
        },
        {
          "name": "baseURL",
          "label": "Base URL",
          "type": "string",
          "required": false,
          "description": "Overrides the API endpoint (for proxies, Azure, or OpenAI-compatible servers)."
        }
      ],
      "sources": []
    },
    {
      "type": "llm-gemini",
      "label": "Gemini",
      "icon": "Sparkles",
      "category": "llm",
      "settings": [
        {
          "name": "apiKey",
          "label": "API key",
          "type": "string",
          "required": true,
          "description": "Authenticates with the Gemini API; source from ${GEMINI_API_KEY}. Never logged."
        },
        {
          "name": "model",
          "label": "Model",
          "type": "string",
          "required": false,
          "default": "gemini-3.5-flash",
          "description": "Model id."
        },
        {
          "name": "maxTokens",
          "label": "Max tokens",
          "type": "number",
          "required": false,
          "description": "Default response token cap (0 = the model default); a request may override it."
        },
        {
          "name": "baseURL",
          "label": "Base URL",
          "type": "string",
          "required": false,
          "description": "Overrides the API endpoint (for proxies or testing)."
        }
      ],
      "sources": []
    },
    {
      "type": "slack",
      "label": "Slack",
      "icon": "Slack",
      "settings": [
        {
          "name": "botToken",
          "label": "Bot token",
          "type": "string",
          "required": true,
          "description": "Bot User OAuth token (xoxb-...) used to call the Web API."
        },
        {
          "name": "signingSecret",
          "label": "Signing secret",
          "type": "string",
          "required": false,
          "description": "Verifies inbound Slack request signatures; required to receive events."
        },
        {
          "name": "timeout",
          "label": "Timeout",
          "type": "string",
          "required": false,
          "default": "30s",
          "description": "Bounds each Slack Web API call."
        }
      ],
      "sources": []
    }
  ]
}
