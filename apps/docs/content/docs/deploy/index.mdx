---
title: "Deployment Overview"
description: "Ways to deploy Octo: local k3d, Helm on any Kubernetes, or GCP with Terraform."
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Cards, Card } from 'fumadocs-ui/components/card';

Octo's platform runs on Kubernetes: the editor UI, the orchestrator, the log
aggregator, Postgres, and NATS, with integrations deployed as pods at runtime.
There are three ways to get that stack running, from a laptop to a public
deployment.

<Callout type="info">
  You do not need a cluster to try Octo. The standalone editor ships as a
  single public Docker image with the runtime bundled — see
  [Running from Docker](/editor/docker). This section is about deploying the
  full platform.
</Callout>

## Choose a deployment path

| | Local cluster (k3d) | Helm chart | GCP reference (Terraform) |
|---|---|---|---|
| **Best for** | Development and evaluation | Any existing Kubernetes cluster | A public, TLS-terminated deployment |
| **What you get** | The full stack on `localhost`, with an optional hot-reload dev loop | The full stack, configured through chart values | Single-VM k3s with Traefik, Let's Encrypt TLS, per-integration subdomains, and Cloud Build releases |
| **Images come from** | Built locally and imported into the cluster | Your registry (published with `task images:push`) | Artifact Registry, built by Cloud Build on version tags |
| **Ingress / TLS** | None — `localhost` port mappings | Traefik Ingress + cert-manager (configurable) | Traefik + cert-manager, wired by Terraform |
| **Prerequisites** | `docker`, `k3d`, `devspace`, `task` | `helm`, a cluster, a registry | `gcloud`, `terraform`, `helm`, `docker`, `task`, a Cloud DNS zone |
| **Bring it up** | `task cluster:deploy` | `helm install` / `helm upgrade` | `task infra:apply`, then `task deploy TAG=…` |

All three paths deploy the same components: the platform (editor UI), the
orchestrator, the log aggregator, Postgres, NATS, and a schema job that applies
`sql/schema.sql`. The orchestrator then creates per-integration workloads at
runtime when you deploy an integration from the editor — see
[Architecture](/platform/architecture) and
[Deployments](/platform/deployments) for how the pieces fit together.

<Cards>
  <Card
    href="/deploy/local-k3d"
    title="Local Cluster (k3d)"
    description="Run the full platform locally with k3d and DevSpace, including hot reload."
  />
  <Card
    href="/deploy/helm"
    title="Helm Chart"
    description="Install the Octo chart on any Kubernetes cluster and configure it through values."
  />
  <Card
    href="/deploy/gcp-terraform"
    title="GCP with Terraform"
    description="The reference deployment: single-VM k3s, Traefik, Let's Encrypt, Cloud Build releases."
  />
</Cards>

## Supporting references

<Cards>
  <Card
    href="/deploy/docker-images"
    title="Docker Images"
    description="Every published image: what it does, where its Dockerfile lives, how it is built."
  />
  <Card
    href="/deploy/releases-and-upgrades"
    title="Releases and Upgrades"
    description="How versions are cut with release-please and how to roll an upgrade."
  />
</Cards>
