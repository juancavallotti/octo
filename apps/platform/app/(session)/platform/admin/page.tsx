import AdminTiles from "./AdminTiles";

/**
 * The admin hub (`/platform/admin`): site-wide settings, reached from the user
 * menu. The header and the section switcher come from the layout.
 */
export default function AdminPage() {
  return <AdminTiles />;
}
