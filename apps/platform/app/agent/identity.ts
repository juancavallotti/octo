/**
 * Who Dr. Octo is, as a fact rather than as configuration.
 *
 * Its own module, and dependency-free, so that both the server and the browser
 * can import it without dragging a client behind it.
 */

/**
 * The agent id Dr. Octo declares in his own definition. A constant rather than a
 * lookup: it is part of the definition, not of an install.
 */
export const DR_OCTO_AGENT_ID = "dr-octo";
