{{- /* ClusterIP: the editor is reached through the Ingress, not a NodePort. */}}
{{- include "octo-common.service" (dict
      "root" $
      "component" "platform"
      "type" "ClusterIP"
      "ports" (list (dict "name" "http" "port" .Values.platform.service.port "targetPort" .Values.platform.service.port))) }}
