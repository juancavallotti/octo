apiVersion: v2
name: octo
description: >-
  octo enterprise-integration platform — the editor UI, the orchestrator API,
  the log aggregator, a NATS broker and Postgres. Ships environment profiles for
  k3d, kind, GKE (Autopilot and Standard) and EKS, and can run against a managed
  database instead of the bundled Postgres.
type: application
version: 0.7.0
appVersion: 0.6.4
dependencies:
  - name: octo-common
    version: 0.6.4
    repository: ''
