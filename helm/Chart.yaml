apiVersion: v2
name: octo
description: >-
  octo enterprise-integration platform — Postgres (StatefulSet), a NATS broker,
  the orchestrator API, and the editor UI, with a Traefik Ingress + cert-manager
  TLS for the editor.
type: application
version: 0.5.0
appVersion: 0.2.0
