# The bundled platform app + runtime (octo-platform image). The Next.js server runs
# on 3000 and spawns the bundled octo binary via OCTO_BIN_PATH for the RUN feature,
# and the bundled dolphin via DOLPHIN_BIN_PATH for the editor's Testing tab.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform
  namespace: octo-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: platform
  template:
    metadata:
      labels:
        app: platform
    spec:
      containers:
        - name: platform
          # Built by `task cluster:images` and loaded via `k3d image import`.
          image: octo-platform:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            # Match the image defaults; OCTO_RUN_DIR is backed by the writable
            # emptyDir below so the editor can write rendered configs at runtime.
            - name: OCTO_BIN_PATH
              value: /usr/local/bin/octo
            # The test runner the editor's Testing tab spawns. Unset would leave
            # the tab able to author suites but not run them.
            - name: DOLPHIN_BIN_PATH
              value: /usr/local/bin/dolphin
            - name: OCTO_RUN_DIR
              value: /app/.octo-run
            # Enables the integration/folder management UI; the editor's BFF
            # proxy routes front the orchestrator (in-cluster service DNS).
            - name: ORCHESTRATOR_URL
              value: http://orchestrator.octo-dev:8090
            # In-cluster NATS broker (deploy/k8s/nats.yaml, port 4222). The BFF
            # subscribes to deployment-status and integration-write subjects and
            # serves them to the browser as SSE (issue #74).
            - name: NATS_URL
              value: nats://nats:4222
            # NATS monitoring HTTP service (deploy/k8s/nats.yaml, port 8222). The
            # /platform/queues view polls /varz + /connz from here directly.
            - name: NATS_MONITOR_URL
              value: http://nats:8222
            # Log-aggregator query API (deploy/k8s/logs.yaml, port 8091). The
            # /platform/logs view reads stored log events from here directly.
            - name: LOGS_URL
              value: http://logs:8091
            # Canonical base URL Auth.js builds sign-in redirects / OAuth callbacks
            # against. Without it, trustHost infers the host from request headers,
            # which behind the k3d loadbalancer/NodePort is 0.0.0.0:3000 — sending
            # sign-in to the wrong origin. Harmless when SSO is off; the URL must
            # match how you reach the cluster (k3d maps localhost:3000 -> :30030).
            # (The GCP deploy sets this from the ingress host via the Helm chart.)
            - name: AUTH_URL
              value: http://localhost:3000
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: octo-run
              mountPath: /app/.octo-run
      volumes:
        - name: octo-run
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: platform
  namespace: octo-dev
spec:
  # NodePort so the k3d loadbalancer mapping (localhost:3000 -> 30030) reaches it.
  type: NodePort
  selector:
    app: platform
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30030
