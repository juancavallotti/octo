# `registry`

Artifact Registry repository.

One Docker-format repository, which holds both the octo images and the OCI Helm
chart — they are the same kind of artifact as far as the registry is concerned.

**Used by:** infra

Inputs and outputs are documented on the `variable` and `output` blocks in
`variables.tf` and `outputs.tf`.
